# J-owned overrides

Store only metadata authored or intentionally maintained by J Launcher here.
Use `overrides/<package uid>/<version>.json`; the refresh tool copies each file
into the published package and updates the required hashes and indexes.

Prism metadata remains the compatibility baseline for versions without a
J-owned override.
